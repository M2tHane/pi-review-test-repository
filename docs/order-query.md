# 订单查询与导出

运营面板按订单加入目录的顺序展示记录；导出文件按订单号排序。导出操作不应改变运营面板随后看到的顺序。

例如，先加入 `ORDER-102`，再加入 `ORDER-101`：导出内容按 `ORDER-101`、`ORDER-102` 排列；再次调用 `listOrders()` 时仍按 `ORDER-102`、`ORDER-101` 展示。

此模块是治理服务完整流程的独立演示数据，使用单线程顺序调用，不连接真实订单系统。
